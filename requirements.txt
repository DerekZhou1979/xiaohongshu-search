# 小红书搜索工具 V2 - 核心依赖包（已清理优化）
# 经过环境清理和版本验证，确保最佳兼容性和性能
# 更新时间: 2025-06-26

# ===========================================
# Web 框架 (Flask + CORS)
# ===========================================
Flask==3.1.1
flask-cors==6.0.1

# ===========================================
# 爬虫核心依赖
# ===========================================
# Selenium WebDriver 自动化
selenium==4.33.0

# WebDriver 管理器 (支持国内镜像源)
webdriver-manager==4.0.2

# HTTP 请求库
requests==2.32.4

# HTML/XML 解析
beautifulsoup4==4.13.4
lxml==5.4.0

# 网络库 (兼容 LibreSSL) - 修复与selenium的兼容性
urllib3~=2.4.0

# ===========================================
# 图像处理
# ===========================================
pillow==11.2.1

# ===========================================
# 环境配置
# ===========================================
python-dotenv==1.1.1

# ===========================================
# 中文处理
# ===========================================
# 中文分词 (用于关键词提取和内容分析)
jieba==0.42.1

# ===========================================
# 安装说明
# ===========================================
# 建议使用国内镜像源安装，提升下载速度：
# pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple/
#
# 或者配置永久镜像源：
# pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple/

# ===========================================
# 环境要求
# ===========================================
# Python: >= 3.9
# Chrome浏览器: 最新版本
# 操作系统: macOS/Linux/Windows
# 内存: >= 4GB (推荐 8GB)

# ===========================================
# 功能特性说明
# ===========================================
# ✅ 已移除所有不相关的包 (mitmproxy、aiohttp、fastapi等)
# ✅ 移除异步网络库依赖 (aiohappyeyeballs、aiosignal等)  
# ✅ 移除不必要工具包 (loguru、protobuf、tornado等)
# ✅ 保留最小必要依赖集合 (仅11个核心包)
# ✅ 支持 ChromeDriver 自动下载和管理
# ✅ 兼容 macOS LibreSSL 环境
# ✅ 优化启动速度和内存占用
# ✅ 减少依赖冲突和版本问题

# ===========================================
# 可选扩展功能 (目前未启用)
# ===========================================
# 如果需要AI生成功能，可以取消下面相关依赖的注释：

# OpenAI API
# openai>=1.0.0

# 百度文心一言 API  
# qianfan>=0.0.5

# 阿里云通义千问 API
# dashscope>=1.0.0

# 腾讯云混元 API
# tencentcloud-sdk-python>=3.0.0

# 本地大模型 (ollama)
# ollama>=0.1.0

# 图像识别和OCR
# opencv-python>=4.8.0
# pytesseract>=0.3.10

# 数据库支持
# sqlite3  # Python内置
# pymongo>=4.0.0  # MongoDB
# redis>=4.5.0    # Redis

# ===========================================
# 故障排除
# ===========================================
# 如果遇到 urllib3 警告，这是正常现象，不影响功能
# 如果 ChromeDriver 下载失败，程序会自动尝试多种方式
# 如果权限问题，请确保有读写 cache/ 目录的权限
